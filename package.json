{
  "name": "alias-for-gmail",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Gmail custom-domain sender using your own provider account.",
  "license": "MIT",
  "scripts": {"test": "node --test tests/*.test.mjs", "check": "node scripts/audit.mjs && npm test"},
  "devDependencies": {"jsdom": "29.1.1"},
  "engines": {"node": ">=22.12"}
}
